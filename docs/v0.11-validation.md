# ZeroSound v0.11 validation record

Status: automated verification passed on 2026-08-11; physical office validation pending

## Automated verification

- `swift build -Xswiftc -warnings-as-errors`: passed.
- `xcrun swift-format lint --recursive Sources Tests`: passed.
- `git diff --check`: passed.
- `swift test`: 72 tests passed.
- `swift test --sanitize=thread`: 72 tests passed with no reported data races.
- `swift test --sanitize=address`: 72 tests passed with no reported memory errors.
- Universal release archive: both `arm64` and `x86_64`, nested ad-hoc signatures valid, ZIP integrity
  passed, version `0.11.0` build `13`.
- The exact bundled release candidate launched successfully and remained healthy during a local
  process smoke test; the installed production app was restored afterward.
- Archive SHA-256: `5d7abffdc277fe23983defa94e3bc7b319722b4feddaab255d461c4de7d9a983`.

The suite covers:

- arbitrary split/coalesced TCP frames and malformed/truncated bounds;
- explicit control/audio join state, authenticated UDP registration, and authoritative roster
  publication;
- abrupt disconnect cleanup, eight-member joining, source transfer, and non-source join/leave during
  live audio;
- authenticated in-session UDP path replacement without leaving the room or changing stream state;
- silent TCP join timeout and a clean retry through the same connection owner, with stale timeout
  fencing;
- coordinator exit, successor promotion, survivor reconnection, clock relock, fresh stream generation,
  and resumed UDP audio;
- stale authority/source/generation fencing and prevention of competing coordinator sessions;
- bounded latest-value audio sends and the exact 1,008-byte production datagram budget;
- 20,000-packet audio-plane saturation while reliable room control remains responsive, plus 16
  repeated graceful/abrupt transport lifecycles with complete cleanup and fresh stream fencing;
- packet loss, reorder, burst concealment, underrun recovery, output-device recovery, and fresh
  room-time re-anchoring;
- five deterministic 90-second fault seeds with independent source, room, and output-clock skew,
  random single/burst loss, and live clock-map corrections, all ending below 3 ms software phase;
- two deterministic four-hour virtual soaks with independent clocks, randomized loss bursts, and
  repeated live room-clock corrections;
- backwards-compatible decoding of diagnostics from previous app versions.

## Physical release gates

Use [`office-validation-runbook.md`](office-validation-runbook.md) and record results below. The goal
is not complete and v0.11 must not be published to the update feed until these are measured.

| Gate | Result | Evidence |
| --- | --- | --- |
| Two physical Macs | Pending | |
| Five physical Macs | Pending | |
| Eight physical Macs | Pending | |
| Coordinator exit during live audio | Pending | |
| Sleep/wake, Wi-Fi interruption, output change | Pending | |
| Multi-hour mixed-hardware soak | Pending | |
| Previous production build updates to v0.11 | Pending | |

For failures, copy diagnostics from every Mac and include the approximate audible-event time. The
report now separates TCP control state, UDP audio state, join stage, before-send drops, clock skew,
phase error, correction rate, queue depth, rolling loss, underruns, resynchronizations, and the last
recovery reason.
