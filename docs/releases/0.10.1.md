# ZeroSound 0.10.1

- Corrects the AVAudioPlayerNode timeline origin so scheduled playback no longer enters a false phase-resynchronization loop.
- Keeps room health in a recovering state while playback phase is still being measured instead of briefly reporting a false Excellent state.
- Adds regression coverage for player-start scheduling, explicit sample origins, and live-health transitions.
